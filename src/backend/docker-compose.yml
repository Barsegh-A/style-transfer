version: '3'
services:
  request_handler:
    build: ./apiserver
    depends_on:
      - redis
    networks:
      - app-network

  queue_processor:
    build: ./styletransfer
    depends_on:
      - redis
    networks:
      - app-network

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"  # default Redis port
    volumes:
      - redis-data:/data  # persistent storage for Redis
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  redis-data:
